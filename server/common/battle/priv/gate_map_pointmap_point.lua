local map_point = GameData.gate_map_point.map_point

table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
table.insert(map_point, {
	})
